# Vercel Blob Storage Configuration
# You can get these values from your Vercel dashboard under Storage > Blob
BLOB_READ_WRITE_TOKEN=your_blob_read_write_token_here

# Optional: Custom base URL for CLI (defaults to http://localhost:3000)
BLOBZIP_URL=http://localhost:3000

# Production URL (when deployed to Vercel)
# BLOBZIP_URL=https://blob.zip